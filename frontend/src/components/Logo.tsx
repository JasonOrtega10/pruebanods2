const Logo = ({ height, width }: { height: number; width: number }) => {
  return (
  
           <img
              src="/img/logo.png"
              alt="Nodeny Logo"
              width={width}
              height={height}
            />
  );
};
export default Logo;
